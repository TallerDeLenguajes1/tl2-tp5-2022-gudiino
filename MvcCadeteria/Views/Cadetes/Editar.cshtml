@model MvcCadeteria.ViewModels.EditarCdtViewModel

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar Cadete</h1>

<h4></h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Editar">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="@Model.id" />
            <div class="form-group">
                <label asp-for="@Model.nombre" class="control-label" ></label>
                <input asp-for="@Model.nombre" class="form-control" value="@Model.nombre"/>
                <span asp-validation-for="@Model.nombre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.domicilio" class="control-label"></label>
                <input asp-for="@Model.domicilio" class="form-control" value="@Model.domicilio"/>
                <span asp-validation-for="@Model.domicilio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.telefono" class="control-label"></label>
                <input asp-for="@Model.telefono" class="form-control" value="@Model.telefono"/>
                <span asp-validation-for="@Model.telefono" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.sucursal" class="control-label"></label>
                <input asp-for="@Model.sucursal" class="form-control" value="@Model.sucursal"/>
                <span asp-validation-for="@Model.sucursal" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
